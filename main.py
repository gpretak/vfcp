from vfcp_calc import vfcp_calc

# vfcp_calc has two argurments
# vfcp_calc(input_filename,output_filename)
# input_filename is the filename in the input folder without the .xlsx
# output_filename is the filename in the output folder, the exported file with vfcp amount, without the .xlsx

vfcp_calc('test_vfcp','testoutput_0705')
